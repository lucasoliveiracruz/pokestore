export default interface DefaultTranslations {
  header: {
    title: string;
    search_placeholder: string;
  };
  card_item: {
    add: string;
  };
  cart: {
    total: string;
    cart: string;
    conclude: string;
    order: string;
  };
  modal_success: {
    title: string;
    message: string;
    button: string;
  };
}
